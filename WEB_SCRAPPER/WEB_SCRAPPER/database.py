#import sqlite3

#conn = sqlite3.connect('my_quotes.db')
#curr = conn.cursor()

# curr.execute("""create table quotes_table( title text, author text, tag text )""")

#curr.execute("""insert into quotes_table values ("python web scrapper and crawler", "web scrapper", "crawl and scrap")""")

#conn.commit()
#conn.close()